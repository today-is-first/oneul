package store.oneul.mvc.config;

public enum ErrorCode {
    INTERNAL_SERVER_ERROR,
    USER_NOT_FOUND,
    INVALID_PARAMETER,
    DUPLICATE_EMAIL,
    IMAGE_UPLOAD_FAIL,
    // 필요에 따라 추가
    NOT_FOUND
} 